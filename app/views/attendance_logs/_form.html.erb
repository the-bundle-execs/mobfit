<%= form_with(model: attendance_log, local: true) do |form| %>
  <% if attendance_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attendance_log.errors.count, "error") %> prohibited this attendance_log from being saved:</h2>

      <ul>
      <% attendance_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
